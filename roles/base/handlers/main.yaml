---
- name: restart paccache
  systemd:
    name: paccache.timer
    state: restarted
  when: ansible_is_chroot == False

- name: start paccache
  systemd:
    name: paccache.timer
    state: started
  when: ansible_is_chroot == False

- name: start user systemd instance
  systemd:
    name: "user@{{ user.uid }}"
    state: started
  when: ansible_is_chroot == False
